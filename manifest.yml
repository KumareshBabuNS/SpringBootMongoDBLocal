applications:
 - name: pas-mongodb-local
   memory: 450M
   path: ./target/demo-0.0.1-SNAPSHOT.jar
   instances: 1
   host: pas-mongodb-local
   buildpack: java_buildpack
   services:
    - pas-mongodb
